import React from "react";
import logo from '../logo.svg';

export default function Navbar() {
    return (
        <nav className="App-header">
            <img src={logo} className="App-logo" alt="logo" />
            <h1>open movie db search</h1>

        </nav>
    )

}